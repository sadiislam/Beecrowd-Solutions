#include<stdio.h>
int main()
{
    float a,b,sum;
    scanf("%f %f",&a,&b); //taking float
    sum=(a*3.5 + b*7.5) / (3.5 + 7.5); //avarage calculating
    printf("MEDIA = %.5f\n",sum);
    return 0;
}

